# R33 Architect Review — GPT-5.5

## Verdict
REQUEST_MAJOR_CHANGES

## Critical (必须修复，否则 BLOCK) (B1..Bn)
- B1: TickCommitRecord / TickTrace envelope 的权威字段集合不一致，导致 replay-critical 边界无法实现。`api-registry.md` 声明 TickTrace Envelope 共 22 字段，包含 `system_manifest_hash`、`limits_manifest_hash`、`host_abi_version`、`visibility_truncation_version` 等；但 `05-persistence-contract.md` 将 TickCommitRecord 缩成 10 个 FDB 原子字段；`engine.md` 又列出 `TickInputEnvelope` 包含 `collect_id`、`attempt_id`、`commit_id`、`fuel_schedule_version`、`host_cost_table_version`、`terminal_state` 等；`01-tick-protocol.md` 还使用旧字段 `wasm_status`。这些不是同一对象的清晰分层，而是多个相互重叠的“权威 envelope”。影响：replay verifier、FDB schema、hash chain、CI fixture 和 SDK/registry codegen 无法知道哪些字段必须同事务持久化，哪些字段只是 rich/debug；实现者会各自选择字段集，最终 replay 与线上状态分叉。修复建议：定义单一 `TickCommitRecord` schema，明确包含 replay 必需的所有版本化字段；将 `TickInputEnvelope`、`TickTrace Envelope`、`ReplayArtifact` 三者关系画成字段映射表；`api-registry.md`、`engine.md`、`01-tick-protocol.md`、`05-persistence-contract.md` 只能引用该 schema，不再各自列字段。(/data/swarm/docs/specs/reference/api-registry.md:643; /data/swarm/docs/specs/core/05-persistence-contract.md:34; /data/swarm/docs/design/engine.md:271; /data/swarm/docs/specs/core/01-tick-protocol.md:543)
- B2: Persistence Contract 内部同时声称对象存储数据非 replay-critical，又在“正常 Replay”流程中要求读取对象存储 tick_trace_blob 并反序列化 TickCommitRecord，语义自相矛盾。前文说 deterministic replay 只需 FDB 中 TickCommitRecord 10 字段 + keyframe/delta chain，WASM module blob 与 RichTraceBlob 缺失不影响 replay；但 §5.1 正常 Replay 第 2–5 步要求从对象存储获取 tick_trace_blob、验证 blob hash、反序列化 TickCommitRecord；§7.2 又把 blob 损坏归类为可能 `unreplayable`，与“对象存储中不存 replay-critical 数据”冲突。影响：一旦对象存储失败，系统到底是 `audit_gap` 继续 replay，还是 `unreplayable` 断链，没有确定合同；这会直接破坏反作弊、冷存储 GC 和灾备恢复策略。修复建议：把 replay 流程改为“先读 FDB TickCommitRecord + keyframe/delta，RichTraceBlob 仅增强 debug”；`unreplayable` 只允许由 FDB replay-critical 字段或 keyframe/delta chain 缺失触发，不能由 RichTraceBlob 缺失触发。(/data/swarm/docs/specs/core/05-persistence-contract.md:38; /data/swarm/docs/specs/core/05-persistence-contract.md:58; /data/swarm/docs/specs/core/05-persistence-contract.md:213; /data/swarm/docs/specs/core/05-persistence-contract.md:320)
- B3: Phase 2a/2b 职责边界对 Attack/RangedAttack/Heal 存在根本冲突。`engine.md` 明确 Phase 2a Attack/RangedAttack 直接应用 damage、立即反映到 HP，Phase 2b combat_system 只处理 Tower/DoT；但 `06-phase2b-system-manifest.md` 同时把 `Attack/RangedAttack/Heal` 列为 S01 handled commands，又把 S11-S13 定义为 Combat Parallel Set，读 Drone/Entity 并写 PendingDamage/PendingHeal，S15 作为 HitPoints unique writer；R/W 矩阵还标记 S01 写 HitPoints、S15 也写 HitPoints。影响：同一玩家攻击可能被 2a inline 和 2b combat 双重处理，或实现者不知道 HP unique writer 到底是 S01 还是 S15；这会破坏先到先得语义、并行安全证明和 determinism。修复建议：二选一并全局统一：方案 A，玩家 Attack/Heal 在 Phase 2a 仅产生命令效果 intent，S11-S15 统一结算 HP；方案 B，玩家 Attack/Heal 完全 inline 应用，S11-S15 只处理非玩家自动战斗且从 handled commands/RW 矩阵中移除玩家命令。(/data/swarm/docs/design/engine.md:245; /data/swarm/docs/specs/core/06-phase2b-system-manifest.md:92; /data/swarm/docs/specs/core/06-phase2b-system-manifest.md:181; /data/swarm/docs/specs/core/06-phase2b-system-manifest.md:209; /data/swarm/docs/specs/core/06-phase2b-system-manifest.md:391)
- B4: Phase 2b manifest 自称“31 systems”，但实际编号与清单只有 S01–S29，且 S22a/S22b 是插入型子编号；Manifest Hash 却按 `system_id_31` 计算。影响：system manifest hash、CI “31 systems” 校验、TickTrace 中的 `system_manifest_hash` 无法稳定生成；实现层如果按 29 个编号注册而文档按 31 个系统哈希，replay 会因 manifest mismatch 失败。修复建议：建立规范的系统 ID 序列，避免 `S22a/S22b` 这种非线性编号；若总数为 31，则显式列出 S01–S31；若总数为 29，则所有“31 systems”声明、hash 示例和 CI gate 改为 29，并明确 buffer producer 是否计为独立 system。(/data/swarm/docs/specs/core/06-phase2b-system-manifest.md:20; /data/swarm/docs/specs/core/06-phase2b-system-manifest.md:76; /data/swarm/docs/specs/core/06-phase2b-system-manifest.md:145; /data/swarm/docs/specs/core/06-phase2b-system-manifest.md:440)
- B5: Sandbox host function registry 不闭合：API Registry 注册 6 个 host functions，包含 `host_get_random`；WASM Sandbox 允许函数清单只有 5 个，且 RNG 章节又要求 WASM 必须使用 `swarm_get_random(sequence)` 从 host 获取确定性随机数。影响：WASM 玩家是否能获取确定性随机数没有可实现合同；如果实现按 sandbox 白名单执行，`host_get_random` 被拒；如果按 registry 执行，sandbox 的导入白名单不完整，安全审计与 ABI codegen 分叉。修复建议：以 API Registry 或独立 Host ABI IDL 为机器权威，Sandbox §3.2 必须完整引用同一清单；统一名称为 `host_get_random` 或 `swarm_get_random`，并补齐预算、输出上限、错误优先级与 replay 字段。(/data/swarm/docs/specs/reference/api-registry.md:455; /data/swarm/docs/specs/core/04-wasm-sandbox.md:202; /data/swarm/docs/specs/core/01-tick-protocol.md:960)
- B6: 经济与规则配置仍含浮点字段，违反“禁止 f64 / 全定点”的确定性合同。`08-resource-ledger.md` 原则要求费率用 basis points/ppm、禁止浮点数，但同一参数表仍有 `allied_daily_cap_world_multiplier = 1.0 float`；`07-world-rules.md` 的 world.toml 示例大量使用 `decay_rate = 0.0/0.001`、`transfer_to_global_cost = 0.01`、`damage_multiplier = 1.0`、`special_param = 2.0/0.5`、`default_resistance = 1.0`，并在 mod config 类型中允许 `f64`。影响：规则模组、经济账本、伤害抗性在跨平台 replay 中会出现浮点解析与舍入差异；这不是实现细节，而是核心 determinism contract 破裂。修复建议：所有 world.toml 示例和 schema 改为 `*_bps`、`fixed<u32,N>` 或 ppm；删除 mod config 的 `f64` 类型；为 `special_param`、resistance、damage_multiplier、transfer fee 提供定点字段名和单位。(/data/swarm/docs/specs/core/08-resource-ledger.md:13; /data/swarm/docs/specs/core/08-resource-ledger.md:80; /data/swarm/docs/specs/core/07-world-rules.md:49; /data/swarm/docs/specs/core/07-world-rules.md:103; /data/swarm/docs/specs/core/07-world-rules.md:937; /data/swarm/docs/specs/core/07-world-rules.md:1172)

## High (强烈建议修复) (H1..Hn)
- H1: CommandAction / validation 文档仍重复并分叉权威表，破坏 API Registry 单事实源。`api-registry.md` 明确 CommandAction 表由 IDL 自动生成且总数 21，其他文档只能引用；但 `02-command-validation.md` 又在 §3 和 §8 手写逐指令 schema、特殊攻击参数、冷却、资源消耗，并仍引用 `specs/gameplay/08-api-idl` 作为特殊攻击 IDL 来源。影响：新增/修改 action 时 registry、validation matrix、SDK 和 manifest 很容易不一致；当前文档已出现 `MainActionQuotaExceeded` 不在 registry canonical RejectionReason 中的问题。修复建议：`02-command-validation.md` 保留 validation 条件矩阵，但 action 参数、variant list、错误码枚举必须引用 registry/IDL；任何手写 variant 表改成“从 registry 生成/摘录，不作为权威”。(/data/swarm/docs/specs/reference/api-registry.md:37; /data/swarm/docs/specs/core/02-command-validation.md:152; /data/swarm/docs/specs/core/02-command-validation.md:428; /data/swarm/docs/specs/core/02-command-validation.md:535; /data/swarm/docs/specs/core/02-command-validation.md:665)
- H2: Snapshot Contract 与 Tick Protocol 对 snapshot 生成对象粒度不一致：Snapshot Contract 说“每 tick 结束时为每个 drone 生成 perception snapshot”，Tick Protocol / engine 则是 tick 开始构建全世界快照、按房间分片、按玩家拼接；`09-snapshot-contract.md` JSON 示例还使用 `drone_id`，而 `01-tick-protocol.md` 的 build_snapshot 签名是 `player_id`。影响：WASM 输入到底是 per-player 还是 per-drone 不清楚，会影响 256KB cap、关键实体定义、视野拼接、host_get_objects_in_range 预算和 replay hash。修复建议：统一为 per-player snapshot（若这是目标架构），将 `drone_id` 示例改为 `player_id` + optional actor context；明确多 drone 玩家共享同一 snapshot 还是每 drone 独立感知视图。(/data/swarm/docs/specs/core/09-snapshot-contract.md:21; /data/swarm/docs/specs/core/09-snapshot-contract.md:34; /data/swarm/docs/specs/core/01-tick-protocol.md:136; /data/swarm/docs/design/engine.md:188)
- H3: Worker pool / CPU budget 模型存在单位与容量推导混乱。`engine.md` 的 hard cap 推导把 `1000 players × 5ms = 5000ms` 再除以 40 cores 得到 125ms，但同时 worker_pool_max 默认 256；`api-registry.md` 又声明 sandbox CPU cgroup 为每 3s 0.25s，但 per-player sandbox deadline 是 2500ms；`04-wasm-sandbox.md` 同样配置 `cpu.max = 250000 3000000`。影响：per-player deadline、fuel quota、cgroup CPU quota、worker 并行度之间没有直观一致的 admission contract；运营商无法判断 worker pool 扩容何时有效，何时受 cgroup 限制。修复建议：把 wall-clock deadline、fuel quota、cgroup CPU share、worker_pool_max/hard_cap 拆成不同层：deadline 只控制挂起，fuel 控制公平，cgroup 控制隔离；容量推导必须使用实际 worker_pool_max 或明确 hard cap 场景需配置 `worker_pool_max=1000`。(/data/swarm/docs/design/engine.md:321; /data/swarm/docs/design/engine.md:357; /data/swarm/docs/design/engine.md:393; /data/swarm/docs/specs/reference/api-registry.md:559; /data/swarm/docs/specs/core/04-wasm-sandbox.md:255)
- H4: RuleMod 与核心引擎边界表述互相冲突。`07-world-rules.md` 开头说模组通过 `actions` 请求引擎操作、不能绕过 Command Validation Pipeline；Rhai 事务模型又说 `actions.deduct/award/set_world_param/set_entity_flag` 写入世界状态但不能直接 ECS；后文“核心引擎不知道规则存在”又说规则 System 可以“修改 ECS 资源/组件”。影响：Rhai mod 到底是只能通过 ledger/action buffer 修改世界，还是可以注册 Bevy system 修改 ECS，边界不清会导致第二套状态修改路径，与 determinism/replay 审计冲突。修复建议：把 RuleMod 明确分成“声明式 world parameter / ledger action / event hook”三类能力；禁止或严格登记任何直接 ECS component writer，并要求进入 system manifest/hash；`核心引擎不知道规则存在`应改为“核心引擎通过 RuleMod ABI 调用已登记 hook”。(/data/swarm/docs/specs/core/07-world-rules.md:15; /data/swarm/docs/specs/core/07-world-rules.md:312; /data/swarm/docs/specs/core/07-world-rules.md:354; /data/swarm/docs/specs/core/07-world-rules.md:1218)

## Medium (建议关注) (M1..Mn)
- M1: `design/engine.md` §3.1a 仍使用 “目标 MVP = 500 活跃玩家”“远期方向”等阶段/路线措辞，与“设计即目标状态”的评审原则不一致，也容易让容量合同看起来只是阶段性目标。建议改为目标状态表达：单 Engine target/hard cap、room-partition target、multi-engine shard compatibility，而非 MVP/远期。(/data/swarm/docs/design/engine.md:166)
- M2: `tech-choices.md` 对 Bevy 的理由称 “WASM 沙箱和 Bevy 共享同一 allocator”，但架构中 sandbox worker 是独立进程 + Wasmtime Store reset，并不与 Bevy world 共享 allocator。该理由会误导内存隔离模型。建议改为“同为 Rust 生态、类型与 tooling 协同”，删除 allocator 共享说法。(/data/swarm/docs/design/tech-choices.md:18; /data/swarm/docs/specs/core/04-wasm-sandbox.md:18)
- M3: `09-snapshot-contract.md` 仍保留 “MVP Economy Boundaries” 标题与 Future/MVP 语言，其中部分内容已被 R27 最终设计裁决覆盖。建议改名为 “Core Economy Boundaries” 或 “Economy Scope Contract”，避免后续 reviewer/实现者误读为阶段规划。(/data/swarm/docs/specs/core/09-snapshot-contract.md:181)
- M4: `01-tick-protocol.md` 的资源竞争示例仍使用 `SourceEmpty`、`TileOccupied`、`AlreadyFullHealth`、`TargetFull` 等旧错误名，虽然 validation 文档说明旧码应进 debug_detail。建议替换成 canonical RejectionReason + debug_detail 示例，保持错误语义直观一致。(/data/swarm/docs/specs/core/01-tick-protocol.md:334; /data/swarm/docs/specs/core/02-command-validation.md:154)
- M5: `api-registry.md` 变更记录称 MCP tools “总数为 56 active”，正文则显示 Game API 57 tools + auth_api 11，容易与 CI count gate 产生误读。建议明确 active/tools/alias/skipped 的计数口径。(/data/swarm/docs/specs/reference/api-registry.md:262; /data/swarm/docs/specs/reference/api-registry.md:936)

## Low / Nits (可选改进) (L1..Ln)
- L1: 多处 Markdown 表格存在多余 `|` 或缩进错位，影响生成器/静态检查器解析，例如 RawCommand 字段表、validation matrix、world-rules 字段说明。建议运行 markdown table lint 或生成结构化表。(/data/swarm/docs/specs/core/02-command-validation.md:121; /data/swarm/docs/specs/core/07-world-rules.md:603)
- L2: `design/README.md` 的回放数据路径仍写 `/tick/{N}/state` 完整状态，与 engine/FDB 小事务 + keyframe/delta/object store 的目标模型不够一致。建议改为 `/tick/{N}/commit_record`、`keyframe/delta pointer` 等分层名称。(/data/swarm/docs/design/README.md:177)
- L3: `tech-choices.md` 的 Blake3 XOF 示例 `update_with_seek` 看起来像非标准 API 表达，建议改成抽象伪代码或准确 crate API，避免实现者复制错误。(/data/swarm/docs/design/tech-choices.md:162)

## Strengths (设计亮点)
- S1: Worker Pool 的水平扩展、非瓶颈、per-player deadline 独立性在 `engine.md` 和 `01-tick-protocol.md` 中已经被明确写出，整体方向正确；真正瓶颈被归因到 EXECUTE/平均 WASM 执行时间，而不是简单归咎 worker 数量。(/data/swarm/docs/design/engine.md:342; /data/swarm/docs/specs/core/01-tick-protocol.md:800)
- S2: Shadow Write + Atomic Publish 明确修复了 room-partition 的全局原子性问题，把 staging 与 committed 路径分离，并禁止 per-room partial commit 语义。这是比“单 FDB 大事务”更符合目标容量的架构。(/data/swarm/docs/specs/core/01-tick-protocol.md:393; /data/swarm/docs/specs/core/05-persistence-contract.md:351)
- S3: Phase 2b manifest 引入 serial spine、parallel sets、R/W matrix、unique writer contract 和 manifest hash，方向上是正确的确定性 ECS 设计；问题主要在当前清单内部计数和 HP writer 分工未闭合。(/data/swarm/docs/specs/core/06-phase2b-system-manifest.md:9; /data/swarm/docs/specs/core/06-phase2b-system-manifest.md:387)
- S4: API Registry 的“IDL YAML 为机器权威、Markdown 由生成器输出”原则非常好，适合作为跨 SDK、MCP、host ABI 和 validation 的单事实源。需要做的是让其他文档真正停止重复声明。(/data/swarm/docs/specs/reference/api-registry.md:1)
- S5: Snapshot truncation 的 critical entity 不可截断、farthest-first、omitted statistics 与 degraded tick 标记，体现了竞技公平与可解释性的良好架构取舍。(/data/swarm/docs/specs/core/09-snapshot-contract.md:52; /data/swarm/docs/specs/core/09-snapshot-contract.md:80)
- S6: Resource Ledger 将 local/global/allied/PvE/recycle/build/spawn/upkeep/tax 聚合到单入口，具备良好的审计与反逃逸路径基础。(/data/swarm/docs/specs/core/08-resource-ledger.md:9; /data/swarm/docs/specs/core/08-resource-ledger.md:18)

## CrossCheck — 需要跨方向检查
- CX1: B1/B2 涉及 TickCommitRecord、ReplayArtifact、RichTraceBlob 的字段级安全与审计语义 → 建议 Security 检查 replay-critical 字段缺失时的反作弊可用性、WASM blob 缺失是否应降级为 audit gap。
- CX2: B5 host RNG / Host ABI 不闭合 → 建议 Sandbox 检查 `host_get_random` 是否应暴露、其 seed domain separation、fuel cost 与 syscall/WASI 禁用策略是否一致。
- CX3: B6 浮点字段残留 → 建议 Determinism 检查 world.toml schema、Rhai type system、JSON canonical codec 中是否仍允许 f64/f32 或小数字面量进入 game state。
- CX4: B3 HP writer / Attack 双路径冲突 → 建议 Gameplay 检查玩家 Attack、Tower auto attack、DoT、Heal、SpawningGrace 的目标战斗语义应采用 inline 即时还是 deferred reduce。
- CX5: H4 RuleMod ECS 写边界不清 → 建议 Modding/Extensibility 检查 Rhai ABI 是否允许 direct component writer，以及这些 writer 如何进入 system_manifest_hash 和 replay envelope。
- CX6: H3 worker pool CPU/cgroup 语义 → 建议 Performance 检查 500/1000 active player 推导是否需要 benchmark-gated 表达，并统一 fuel、wall-clock、cpu.max 三套预算。